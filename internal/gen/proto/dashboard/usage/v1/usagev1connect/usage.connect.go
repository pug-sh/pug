// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: dashboard/usage/v1/usage.proto

package usagev1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/pug-sh/pug/internal/gen/proto/dashboard/usage/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// UsageServiceName is the fully-qualified name of the UsageService service.
	UsageServiceName = "dashboard.usage.v1.UsageService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// UsageServiceGetUsageProcedure is the fully-qualified name of the UsageService's GetUsage RPC.
	UsageServiceGetUsageProcedure = "/dashboard.usage.v1.UsageService/GetUsage"
)

// UsageServiceClient is a client for the dashboard.usage.v1.UsageService service.
type UsageServiceClient interface {
	// Returns how many events the org's projects recorded in the current billing
	// period — which runs from the org's signup anniversary, not the 1st of the
	// month — plus the per-project daily series behind it. The numbers come from a
	// scheduled meter whose cadence the deployment sets, so read usage_computed_at
	// for how stale they are rather than assuming one. Both used_events and
	// usage_computed_at are absent if the meter has never run; usage_computed_at
	// alone is present while it is alive but has not summed this period yet.
	GetUsage(context.Context, *connect.Request[v1.GetUsageRequest]) (*connect.Response[v1.GetUsageResponse], error)
}

// NewUsageServiceClient constructs a client for the dashboard.usage.v1.UsageService service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewUsageServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) UsageServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	usageServiceMethods := v1.File_dashboard_usage_v1_usage_proto.Services().ByName("UsageService").Methods()
	return &usageServiceClient{
		getUsage: connect.NewClient[v1.GetUsageRequest, v1.GetUsageResponse](
			httpClient,
			baseURL+UsageServiceGetUsageProcedure,
			connect.WithSchema(usageServiceMethods.ByName("GetUsage")),
			connect.WithClientOptions(opts...),
		),
	}
}

// usageServiceClient implements UsageServiceClient.
type usageServiceClient struct {
	getUsage *connect.Client[v1.GetUsageRequest, v1.GetUsageResponse]
}

// GetUsage calls dashboard.usage.v1.UsageService.GetUsage.
func (c *usageServiceClient) GetUsage(ctx context.Context, req *connect.Request[v1.GetUsageRequest]) (*connect.Response[v1.GetUsageResponse], error) {
	return c.getUsage.CallUnary(ctx, req)
}

// UsageServiceHandler is an implementation of the dashboard.usage.v1.UsageService service.
type UsageServiceHandler interface {
	// Returns how many events the org's projects recorded in the current billing
	// period — which runs from the org's signup anniversary, not the 1st of the
	// month — plus the per-project daily series behind it. The numbers come from a
	// scheduled meter whose cadence the deployment sets, so read usage_computed_at
	// for how stale they are rather than assuming one. Both used_events and
	// usage_computed_at are absent if the meter has never run; usage_computed_at
	// alone is present while it is alive but has not summed this period yet.
	GetUsage(context.Context, *connect.Request[v1.GetUsageRequest]) (*connect.Response[v1.GetUsageResponse], error)
}

// NewUsageServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewUsageServiceHandler(svc UsageServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	usageServiceMethods := v1.File_dashboard_usage_v1_usage_proto.Services().ByName("UsageService").Methods()
	usageServiceGetUsageHandler := connect.NewUnaryHandler(
		UsageServiceGetUsageProcedure,
		svc.GetUsage,
		connect.WithSchema(usageServiceMethods.ByName("GetUsage")),
		connect.WithHandlerOptions(opts...),
	)
	return "/dashboard.usage.v1.UsageService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case UsageServiceGetUsageProcedure:
			usageServiceGetUsageHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedUsageServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedUsageServiceHandler struct{}

func (UnimplementedUsageServiceHandler) GetUsage(context.Context, *connect.Request[v1.GetUsageRequest]) (*connect.Response[v1.GetUsageResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("dashboard.usage.v1.UsageService.GetUsage is not implemented"))
}
