// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: api_keys.sql

package dbwrite

import (
	"context"
)

const createApiKey = `-- name: CreateApiKey :one
insert into api_keys (display_name, id, kind, masked, project_id, token)
values ($1, $2, $3, $4, $5, $6)
returning create_time, display_name, id, kind, masked, project_id, token, update_time
`

type CreateApiKeyParams struct {
	DisplayName string
	ID          string
	Kind        string
	Masked      string
	ProjectID   string
	Token       string
}

func (q *Queries) CreateApiKey(ctx context.Context, arg CreateApiKeyParams) (ApiKey, error) {
	row := q.db.QueryRow(ctx, createApiKey,
		arg.DisplayName,
		arg.ID,
		arg.Kind,
		arg.Masked,
		arg.ProjectID,
		arg.Token,
	)
	var i ApiKey
	err := row.Scan(
		&i.CreateTime,
		&i.DisplayName,
		&i.ID,
		&i.Kind,
		&i.Masked,
		&i.ProjectID,
		&i.Token,
		&i.UpdateTime,
	)
	return i, err
}

const deleteApiKey = `-- name: DeleteApiKey :one
delete from api_keys k
where k.id = $1 and k.project_id = $2
returning k.create_time, k.display_name, k.id, k.kind, k.masked, k.project_id, k.token, k.update_time
`

type DeleteApiKeyParams struct {
	ID        string
	ProjectID string
}

// Scoped by project_id, so a key id belonging to another project can never be
// deleted by guessing it. No row deleted (pgx.ErrNoRows) means "not found in
// this project" — the caller does not distinguish the two.
//
// This row is the only place the key exists, so deleting it is the whole
// revocation. The caller drops the cached project row keyed by its token.
func (q *Queries) DeleteApiKey(ctx context.Context, arg DeleteApiKeyParams) (ApiKey, error) {
	row := q.db.QueryRow(ctx, deleteApiKey, arg.ID, arg.ProjectID)
	var i ApiKey
	err := row.Scan(
		&i.CreateTime,
		&i.DisplayName,
		&i.ID,
		&i.Kind,
		&i.Masked,
		&i.ProjectID,
		&i.Token,
		&i.UpdateTime,
	)
	return i, err
}
