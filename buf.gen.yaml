version: v2
clean: true
plugins:
  # Go plugins (existing)
  - local: ["go", "tool", "protoc-gen-go"]
    out: internal/gen/proto
    opt: paths=source_relative
  - local: ["go", "tool", "protoc-gen-connect-go"]
    out: internal/gen/proto
    opt: paths=source_relative

  # TypeScript plugin (requires: pnpm add -D @bufbuild/protoc-gen-es)
  # - local: protoc-gen-es
  #   out: clients/typescript
  #   opt:
  #     - target=ts
  #     - import_extension=js
